// An Error Page (error.tsx) will always be displayed when an error happens.
// We can have multiple files to act as a fallback in every route level.
const Error = () => {
  return <h2>Ops... something wrong happened!</h2>;
};

export default Error;
